name: Minimal test
on:
  workflow_dispatch:
  push:
    branches: ["main"]
    paths:
      - '.github/workflows/test.yml'
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: hello (no action download needed)
        run: echo "Hello from $GITHUB_REPOSITORY at $(date)"
      - name: try checkout (this needs codeload)
        uses: actions/checkout@v4
